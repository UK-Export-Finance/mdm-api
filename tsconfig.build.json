{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node"],
  },
  "include": ["src", "jest.config.ts"],
  "exclude": [
    // NPM
    "node_modules",
    // Unit, API and E2E tests
    "test",
    "**/*.test.ts",
    "**/*.spec.ts",
    // Build
    "dist",
    // Docker
    "docker-compose*.yml",
    "Dockerfile",
    // Logs
    "logs"
  ]
}
